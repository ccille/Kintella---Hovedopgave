using KintellaLocalizationREST.Model;
using System.Reflection;

namespace KintellaLocalizationREST.Mockdata
{
    public class MockModules
    {
        //public List<Model.Module> allmodules = new List<Model.Module>()
        //{
        //    new Model.Module(1, "Nyheder", new List<SubModule>() { new SubModule(1, "Fisk"), new SubModule(2, "Banan") }),
        //    new Model.Module(2, "Kalender", new List < SubModule > () { new SubModule(3, "Bent kommer forbi"), new SubModule(6, "Ben smutter igen")}),
        //    new Model.Module(3, "Livshistorie", new List < SubModule > () { new SubModule(4, "Diagnoser"), new SubModule(5, "Pårørende") })

        //};

        //public List<Text> alltext = new List<Text>()
        //{
        //    new Text(1, 2, "Hello", "New", "Engelsk"),
        //    new Text(2, 4, "Hello Diagnose", "New Cat", "Engelsk"),
        //    new Text(3, 2, "hewwo", "Newest or something", "English")
        //};

    }

}
